[build]
  # Navigate to monorepo root, install all deps, then build this package
  command = "cd ../.. && pnpm install && pnpm --filter pulsar-mcr build"
  publish = "dist"

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
