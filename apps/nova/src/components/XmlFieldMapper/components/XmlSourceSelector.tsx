// Re-export the JSON SourceSelector as it works the same for XML
// The source selection logic is identical - we just need to select data sources and paths
export { SourceSelector as XmlSourceSelector } from '../../JsonFieldMapper/components/SourceSelector';
