
  # Fusion App (supabase)

  This is a code bundle for Fusion App (supabase). The original project is available at https://www.figma.com/design/WOZG4barqJeFo1mS9qXWvF/Fusion-App--supabase-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  