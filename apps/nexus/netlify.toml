[build]
  command = "cd ../.. && pnpm install && pnpm --filter nexus build"
  publish = "dist"

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
